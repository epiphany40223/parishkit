"""Shared credential and secret-loading helpers.

This package centralizes how ParishKit tools locate and read credentials so
that secrets are loaded at runtime rather than baked into code or committed to
the repository.
"""
